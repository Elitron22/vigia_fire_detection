# Dependencias del runtime ARM64. PyTorch se resuelve como rueda CPU aarch64.
ultralytics==8.4.132
fastapi==0.141.1
uvicorn==0.52.4
# uvicorn necesita una biblioteca WebSocket para la cámara en directo (/api/live).
websockets==15.0.1
httpx==0.28.1
PyYAML==6.0.2
